#应用名称
spring:
  application:
    name: order-service

    #数据库连接
  datasource:
    url: jdbc:mysql://localhost:3306/lerun_order?userSSL=false&serverTimezone=GMT&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 13597803422dw

#开启mybatis的sql日志
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

ribbon:
  http:
    client:
      enabled: true

    #springcloud配置
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
#服务端口
server:
  port: 8082

#全局时间格式
order:
  timeFormat: yyyy-MM-dd HH:mm:ss
